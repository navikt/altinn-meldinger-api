server.servlet.context-path: /altinn-meldinger

spring.profiles.active: local

---

spring.config.activate.on-profile: local
wiremock.port: 8081

altinn:
  uri: http://localhost:8081/altinn
  brukernavn: brukernavn
  passord: passord
